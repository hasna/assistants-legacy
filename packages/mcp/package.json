{
  "name": "@hasna/assistants-mcp",
  "version": "0.1.1",
  "description": "MCP server for running AI assistants",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "assistants-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --format esm",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@hasna/assistants-core": "workspace:*",
    "@hasna/assistants-shared": "workspace:*",
    "@hasna/runtime-bun": "workspace:*",
    "@types/bun": "^1.3.8",
    "typescript": "^5.9.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hasna/assistants.git",
    "directory": "packages/mcp"
  },
  "engines": {
    "bun": ">=1.0"
  }
}
